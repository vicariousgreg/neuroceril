
            # Construct initial environment
            (new Environment 'NIL 'NIL)

            # Construct demo (list of (action . env_updates))
            ((the))
            ((fishermen))
            ((steer))
            ((the))
            ((boat))
            NIL
